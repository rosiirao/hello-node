/**
 * the js is tested for async or defer attr. the server must be server it after a few seconds
 */

document.querySelector('main').insertAdjacentHTML('beforeend', `<p><strong>defer.js</strong> run at: ${Date()}</p>`);
